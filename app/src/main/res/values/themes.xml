<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Gratitude" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/status_bar_color</item>
        <item name="statusBarBackground">@color/status_bar_color</item>
        <item name="statusBarForeground">#000</item>
    </style>

    <style name="Theme.Gratitude" parent="Base.Theme.Gratitude" />

</resources>